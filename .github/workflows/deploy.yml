name: Deploy to Apps Script

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install clasp
        run: npm install -g @google/clasp
      
      - name: Decode and create clasprc
        run: |
          echo "${{ secrets.CLASPRC_BASE64 }}" | base64 -d > ~/.clasprc.json
      
      - name: Decode and create clasp.json
        run: |
          echo "${{ secrets.CLASP_BASE64 }}" | base64 -d > .clasp.json
      
      - name: Push to Apps Script
        run: clasp push -f
